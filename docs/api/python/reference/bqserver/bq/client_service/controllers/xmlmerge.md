## Module `source/bqserver/bq/client_service/controllers/xmlmerge.py`

### Functions

#### `xmlmerge(sources)`

Merge a set of documents

#### `xmlmerge1(sources, resource_type, **kw)`

Merge a set of documents

#### `xmljoin(sources, **join_attributes)`

Join a set of XML documents.
