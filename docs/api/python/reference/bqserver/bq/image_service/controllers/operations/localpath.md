## Module `source/bqserver/bq/image_service/controllers/operations/localpath.py`

returns XML with local path to the processed image

### Classes

#### `LocalPathOperation`

Provides local path for response image

Methods:
- `dryrun(token, arg)`
- `action(token, arg)`
