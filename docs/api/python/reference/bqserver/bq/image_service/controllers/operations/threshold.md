## Module `source/bqserver/bq/image_service/controllers/operations/threshold.py`

Threshold an image
       threshold=value[,upper|,lower|,both]
       ex: threshold=128,both

### Classes

#### `ThresholdOperation`

Threshold an image
threshold=value[,upper|,lower|,both]
ex: threshold=128,both

Methods:
- `dryrun(token, arg)`
- `action(token, arg)`
