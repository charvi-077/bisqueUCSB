## Module `source/bqserver/bq/ingest/api.py`

### Functions

#### `find_server(server)`

#### `uri()`

#### `new_blobs(server, **kw)`

Find the preferred data server and store the data there
Excess named arguments are used as attributes for the image object
