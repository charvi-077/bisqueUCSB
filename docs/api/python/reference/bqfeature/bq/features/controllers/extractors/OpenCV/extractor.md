## Module `source/bqfeature/bq/features/controllers/extractors/OpenCV/extractor.py`

OpenCV Feature

No opencv supprot for bisque.

### Classes

#### `KeyPointFeatures`

Methods:
- `cached_columns()`
- `workdir_columns()`

#### `BRISK`

Initalizes table and calculates the ORB descriptor to be
placed into the HDF5 table.

Methods:
- `calculate(resource)`

#### `ORB`

Initalizes table and calculates the ORB descriptor to be
placed into the HDF5 table.

Methods:
- `calculate(resource)`

#### `SIFT`

Initalizes table and calculates the ORB descriptor to be
placed into the HDF5 table.

Methods:
- `calculate(resource)`

#### `SURF`

Initalizes table and calculates the SURF descriptor to be
placed into the HDF5 table.

Methods:
- `calculate(resource)`

#### `Moments`

Methods:
- `calculate(resource)`

#### `HuMoments`

Methods:
- `calculate(resource)`
