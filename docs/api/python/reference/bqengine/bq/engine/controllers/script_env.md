## Module `source/bqengine/bq/engine/controllers/script_env.py`

Use a script specified by the user to run setup, run, and teardown

### Classes

#### `ScriptEnvironment`

Run an external script for environment prep

Methods:
- `process_config(runner)`
- `create_script(mex)`
- `setup_environment(runner)`
- `teardown_environment(runner)`
