[pytest]
testpaths= bqapi

addopts=--ignore=modules --ignore=bqfeature -p pytest_bisque

markers =
    functional: mark a test as a functional webtest needing a running server
    unit: simple unit tests
